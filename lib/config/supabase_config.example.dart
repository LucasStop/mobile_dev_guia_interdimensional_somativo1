/// Template de credenciais do Supabase.
///
/// Copie este arquivo para `supabase_config.dart` (gitignored) e preencha com
/// os valores reais do projeto `guia-interdimensional`, combinados fora do
/// git. A anon key não é secreta — quem protege os dados é a Row Level
/// Security nas tabelas, não o sigilo desta chave — mas o arquivo com o valor
/// real fica fora do repositório por padrão de segurança do time.
class SupabaseConfig {
  static const url = 'https://SEU_PROJETO.supabase.co';
  static const anonKey = 'SUA_ANON_KEY';
}
